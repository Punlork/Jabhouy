name: jabhouy_core
description: Shared primitives and the Drift schema for Jabhouy. Pure Dart — no Flutter.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  drift: ^2.16.0

dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.16.0
  sqlite3: ^2.9.4
  test: ^1.25.0
  very_good_analysis: ^6.0.0
