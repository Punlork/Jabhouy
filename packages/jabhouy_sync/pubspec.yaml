name: jabhouy_sync
description: The outbox-driven sync engine for Jabhouy. Pure Dart — no Flutter.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  drift: ^2.16.0
  jabhouy_core:
    path: ../jabhouy_core

dev_dependencies:
  sqlite3: ^2.9.4
  test: ^1.25.0
  very_good_analysis: ^6.0.0
